#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Pre-commit hook - add any checks you want here
# For now, we'll just allow commits (build check happens on push)
echo "✅ Pre-commit check passed"
