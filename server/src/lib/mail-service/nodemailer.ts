/**
 * Re-exports @react-email/render for rendering React email templates to HTML.
 * Sending is done via Elastic Email API (see elastic-email.ts and email.service.ts).
 */

import { render } from "@react-email/render";

export { render };
